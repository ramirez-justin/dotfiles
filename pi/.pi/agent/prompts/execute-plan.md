---
description: Use Superpowers executing-plans to carry out an existing plan
argument-hint: "<plan path or instructions>"
---
Use the `executing-plans` skill to execute this plan. Verify each step as you
go. For delegated execution, use exactly one `implementer` or `worker` as the
writer after approval, then review its diff and evidence:

$ARGUMENTS
