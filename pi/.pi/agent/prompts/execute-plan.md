---
description: Use Superpowers executing-plans to carry out an existing plan
argument-hint: "<plan path or instructions>"
---
Use the `executing-plans` skill to execute this plan. Verify each step as you
go. If delegating execution, launch exactly one `implementer` or `worker` as
the writer after approval. Do not edit concurrently in the parent; review the
worker's actual diff and evidence afterward:

$ARGUMENTS
